all:
  hosts:
    # Add your VPS hosts here after provisioning
    # Example:
    # my-vps:
    #   ansible_host: 203.0.113.10
    #   ansible_user: root
  vars:
    ansible_user: root
    ansible_ssh_common_args: '-o StrictHostKeyChecking=accept-new'
