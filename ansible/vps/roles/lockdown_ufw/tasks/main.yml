---
- name: Verify Tailscale is connected
  ansible.builtin.command:
    cmd: tailscale status
  changed_when: false

- name: Remove public SSH access from UFW
  community.general.ufw:
    rule: allow
    port: "{{ ssh_port }}"
    proto: tcp
    delete: true
  failed_when: false
