---
- name: Clone automation repo
  ansible.builtin.git:
    repo: "{{ repo_url }}"
    dest: "/home/{{ control_user }}/automation"
    version: "{{ repo_branch }}"
  become: true
  become_user: "{{ control_user }}"
